apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  namespace: ${{ values.appName }}

spec:
  selector:
    app: ${{ values.appName }}

  ports:
  - port: 80
    targetPort: 8080

  type: ClusterIP